import { PageHeader } from '@/components/layout/PageHeader'

export default function RutasPage() {
  return (
    <div className="space-y-6">
      <PageHeader title="Rutas" subtitle="Rutas programadas del día — en construcción (Fase 2)." />
    </div>
  )
}
